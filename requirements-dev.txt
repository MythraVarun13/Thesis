# Development dependencies (not needed for production pipeline)
# Install with: pip install -r requirements-dev.txt
pytest>=7.0
pytest-cov>=4.0

# Code quality (optional but recommended)
ruff>=0.4
black>=24.0
